# DBeaver 학습 키워드 용어집

> DBeaver 오픈소스를 분석하면서 학습한 개념들을 정리한 문서

---

## 목차
1. [DBeaver가 뭔지 이해하기](#1-dbeaver가-뭔지-이해하기)
2. [JDBC - 데이터베이스 연결의 핵심](#2-jdbc---데이터베이스-연결의-핵심)
3. [Eclipse RCP - DBeaver의 뼈대](#3-eclipse-rcp---dbeaver의-뼈대)
4. [OSGi - 플러그인 시스템의 비밀](#4-osgi---플러그인-시스템의-비밀)
5. [SWT와 UI 기술](#5-swt와-ui-기술)
6. [SQL Dialect - 100개 DB 지원의 비밀](#6-sql-dialect---100개-db-지원의-비밀)
7. [라이선스와 오픈소스](#7-라이선스와-오픈소스)
8. [P2 - 배포와 업데이트](#8-p2---배포와-업데이트)
9. [유사 도구 비교](#9-유사-도구-비교)

---

## 1. DBeaver가 뭔지 이해하기

### 핵심 개념

**DBeaver = JDBC 기반 범용 데이터베이스 클라이언트**

```
┌─────────────────────────────────────────────────────────────┐
│                      DBeaver (클라이언트)                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  UI Layer (Eclipse RCP + SWT)                        │   │
│  │  - SQL 에디터, 테이블 뷰어, ER 다이어그램            │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Model Layer                                         │   │
│  │  - SQL 파싱, 메타데이터 캐싱, 데이터 변환            │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  JDBC Layer                                          │   │
│  │  - 각 DB별 드라이버 로드, 연결 관리                  │   │
│  └─────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────┘
                            │ JDBC 프로토콜
        ┌───────────────────┼───────────────────┐
        ↓                   ↓                   ↓
   ┌─────────┐        ┌─────────┐        ┌─────────┐
   │ MySQL   │        │PostgreSQL│       │ Oracle  │
   │ Server  │        │ Server  │        │ Server  │
   └─────────┘        └─────────┘        └─────────┘
```

### 왜 DBeaver가 필요한가?

| 없을 때 | DBeaver 사용 시 |
|---------|-----------------|
| `mysql -u root -p` 터미널 접속 | GUI로 클릭해서 연결 |
| `SELECT * FROM users;` 직접 입력 | 테이블 클릭하면 데이터 표시 |
| 결과가 텍스트로만 출력 | 스프레드시트처럼 편집 가능 |
| DB마다 다른 클라이언트 필요 | 하나로 모든 DB 접속 |

### DB 클라이언트 vs DB 서버

```
클라이언트 (Client)              서버 (Server)
─────────────────               ─────────────
- DBeaver                       - MySQL Server
- MySQL Workbench               - PostgreSQL Server
- pgAdmin                       - Oracle Database
- DataGrip                      - SQL Server

→ 데이터를 "보여주고 조작"하는 도구   → 데이터를 "저장하고 처리"하는 엔진
→ 사용자 PC에 설치                   → 서버에 설치 (로컬 또는 원격)
→ 없어도 DB 자체는 동작              → 이게 없으면 데이터 저장 불가
```

---

## 2. JDBC - 데이터베이스 연결의 핵심

### JDBC란?

**JDBC = Java Database Connectivity**

Java에서 데이터베이스에 접근하기 위한 **표준 API(인터페이스)**

```java
// JDBC 없이 직접 구현한다면...
MySQLConnection mysqlConn = new MySQLConnection("localhost", "root", "password");
PostgresConnection pgConn = new PostgresConnection("localhost", "admin", "password");
// → DB마다 완전히 다른 코드 필요!

// JDBC 사용 시
Connection conn1 = DriverManager.getConnection("jdbc:mysql://localhost/db", "root", "pw");
Connection conn2 = DriverManager.getConnection("jdbc:postgresql://localhost/db", "admin", "pw");
// → 동일한 인터페이스로 모든 DB 접근!
```

### JDBC 구성 요소

```
┌─────────────────────────────────────────────────────────────┐
│  Java Application (DBeaver)                                 │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  JDBC API (java.sql.*)                                │ │
│  │  - Connection (연결)                                   │ │
│  │  - Statement (SQL 실행)                               │ │
│  │  - ResultSet (결과 조회)                              │ │
│  │  - DatabaseMetaData (메타데이터)                      │ │
│  └───────────────────────────────────────────────────────┘ │
│                           ↓                                 │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  DriverManager                                        │ │
│  │  (어떤 드라이버를 쓸지 결정)                          │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────┬───────────────────────────────┘
                              ↓
         ┌────────────────────┼────────────────────┐
         ↓                    ↓                    ↓
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ MySQL JDBC      │  │ PostgreSQL JDBC │  │ Oracle JDBC     │
│ Driver          │  │ Driver          │  │ Driver          │
│ (mysql-connector│  │ (postgresql.jar)│  │ (ojdbc.jar)     │
│  -java.jar)     │  │                 │  │                 │
└────────┬────────┘  └────────┬────────┘  └────────┬────────┘
         ↓                    ↓                    ↓
    MySQL Server       PostgreSQL Server      Oracle Server
```

### DBeaver에서 JDBC 사용 예시

```java
// DBeaver 내부 코드 (단순화)
// 위치: plugins/org.jkiss.dbeaver.model.jdbc/

public class JDBCDataSource implements DBPDataSource {

    public void initialize(DBRProgressMonitor monitor) {
        // 1. JDBC URL 생성
        String url = "jdbc:postgresql://localhost:5432/mydb";

        // 2. 드라이버 로드 (DBeaver가 자동으로 처리)
        Driver driver = getDriver();

        // 3. 연결 생성
        Connection connection = driver.connect(url, properties);

        // 4. 메타데이터 로딩 (테이블, 컬럼 정보)
        DatabaseMetaData metaData = connection.getMetaData();
        ResultSet tables = metaData.getTables(null, "public", "%", null);

        // 5. 캐시에 저장
        while (tables.next()) {
            String tableName = tables.getString("TABLE_NAME");
            // DBeaver UI에 표시
        }
    }
}
```

### JDBC 드라이버

**드라이버 = DB 벤더가 제공하는 JDBC 구현체**

| DB | JDBC 드라이버 | Maven 의존성 |
|----|--------------|--------------|
| MySQL | mysql-connector-java | `com.mysql:mysql-connector-j` |
| PostgreSQL | postgresql | `org.postgresql:postgresql` |
| Oracle | ojdbc | `com.oracle.database.jdbc:ojdbc11` |
| SQL Server | mssql-jdbc | `com.microsoft.sqlserver:mssql-jdbc` |

DBeaver는 이 드라이버들을 **동적으로 다운로드하고 로드**함

---

## 3. Eclipse RCP - DBeaver의 뼈대

### Eclipse RCP란?

**RCP = Rich Client Platform**

데스크톱 애플리케이션을 만들기 위한 **프레임워크**

```
"Eclipse IDE를 만들 때 사용한 기술을
 다른 애플리케이션도 쓸 수 있게 공개한 것"
```

### RCP 기반 애플리케이션들

```
Eclipse IDE ────┬──── Eclipse RCP (핵심 프레임워크)
                │
DBeaver ────────┤
                │
Spring Tool Suite ──┤
                │
IBM Rational ───┘
```

### RCP 핵심 개념

```
┌────────────────────────────────────────────────────────────────┐
│                        Workbench (작업대)                       │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    Perspective (관점)                      │ │
│  │  ┌─────────────┬─────────────────────────┬────────────┐  │ │
│  │  │   View      │        Editor           │   View     │  │ │
│  │  │ (Navigator) │    (SQL Editor)         │ (Outline)  │  │ │
│  │  │             │                         │            │  │ │
│  │  │ ○ Database  │  SELECT * FROM users    │ ○ Tables   │  │ │
│  │  │   ├ Tables  │  WHERE id = 1;          │ ○ Views    │  │ │
│  │  │   └ Views   │                         │ ○ Indexes  │  │ │
│  │  └─────────────┴─────────────────────────┴────────────┘  │ │
│  └──────────────────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              Toolbar / Menu / Status Bar                  │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

| 용어 | 설명 | DBeaver 예시 |
|------|------|-------------|
| **Workbench** | 전체 애플리케이션 창 | DBeaver 메인 창 |
| **Perspective** | 특정 작업을 위한 View/Editor 배치 | Database Development |
| **View** | 정보를 표시하는 패널 | Database Navigator, Properties |
| **Editor** | 편집 가능한 영역 | SQL Editor, Table Editor |
| **Command** | 실행 가능한 액션 | Execute SQL, Commit |
| **Handler** | Command의 실제 구현 | SQLEditorHandlerExecute |

### DBeaver에서 RCP 활용

```
DBeaver 프로젝트 구조
├── org.jkiss.dbeaver.ui/                    ← 기본 UI
├── org.jkiss.dbeaver.ui.app.standalone/     ← RCP Application 정의
├── org.jkiss.dbeaver.ui.editors.sql/        ← SQL Editor (Editor 확장)
├── org.jkiss.dbeaver.ui.navigator/          ← DB Navigator (View 확장)
└── product/community/                        ← RCP Product 빌드 설정
```

---

## 4. OSGi - 플러그인 시스템의 비밀

### OSGi란?

**OSGi = Open Services Gateway initiative**

Java 모듈 시스템의 표준. **플러그인(번들)을 동적으로 설치/제거/업데이트** 가능

```
"앱을 재시작하지 않고도 기능을 추가하거나 뺄 수 있는 구조"
```

### 왜 OSGi를 쓰는가?

```
일반 Java 애플리케이션                 OSGi 애플리케이션 (DBeaver)
──────────────────────                ─────────────────────────

하나의 거대한 JAR                     작은 번들(플러그인)들의 조합
  ↓                                    ↓
┌──────────────────┐                  ┌──────┐ ┌──────┐ ┌──────┐
│                  │                  │MySQL │ │  PG  │ │Oracle│
│  모든 기능이     │                  │Plugin│ │Plugin│ │Plugin│
│  하나로 뭉쳐있음 │                  └──┬───┘ └──┬───┘ └──┬───┘
│                  │                     │        │        │
└──────────────────┘                  ┌──┴────────┴────────┴──┐
                                      │    Core (공통 기반)    │
                                      └───────────────────────┘

문제점:                               장점:
- 기능 하나 바꾸려면 전체 재빌드      - 필요한 기능만 설치
- 의존성 충돌 (JAR Hell)             - 버전 충돌 방지
- 모든 코드가 서로 접근 가능          - 명확한 모듈 경계
```

### 번들(Bundle) = OSGi 플러그인

```
org.jkiss.dbeaver.ext.postgresql/
├── META-INF/
│   └── MANIFEST.MF          ← OSGi 번들 정의 (핵심!)
├── plugin.xml               ← Eclipse 확장 포인트
├── src/                     ← Java 소스 코드
└── icons/                   ← 리소스
```

### MANIFEST.MF 분석

```properties
# 파일: META-INF/MANIFEST.MF

Bundle-SymbolicName: org.jkiss.dbeaver.ext.postgresql
# → 번들의 고유 ID

Bundle-Version: 1.0.0
# → 버전 (여러 버전 공존 가능!)

Bundle-Activator: org.jkiss.dbeaver.ext.postgresql.PostgreActivator
# → 번들 시작/종료 시 호출되는 클래스

Require-Bundle: org.jkiss.dbeaver.model;bundle-version="1.0.0",
                org.jkiss.dbeaver.model.jdbc
# → 이 번들이 의존하는 다른 번들들

Export-Package: org.jkiss.dbeaver.ext.postgresql.model
# → 다른 번들에게 공개하는 패키지

Import-Package: org.postgresql.jdbc
# → 사용하는 외부 패키지
```

### Extension Point (확장 포인트)

**"플러그인이 꽂힐 수 있는 소켓"**

```xml
<!-- plugin.xml에서 확장 포인트 사용 -->

<!-- 1. dataSourceProvider 확장 포인트에 PostgreSQL 등록 -->
<extension point="org.jkiss.dbeaver.dataSourceProvider">
    <datasource
        id="postgresql"
        class="org.jkiss.dbeaver.ext.postgresql.PostgreDataSourceProvider"
        label="PostgreSQL"
        icon="icons/postgresql_icon.png">
    </datasource>
</extension>
```

```
확장 포인트 시스템:

┌─────────────────────────────────────────────┐
│  org.jkiss.dbeaver.registry                 │
│  ┌───────────────────────────────────────┐  │
│  │  Extension Point: dataSourceProvider  │  │  ← "소켓"
│  │                                       │  │
│  │  (여기에 DB 드라이버들이 꽂힘)        │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
              ↑        ↑        ↑
         ┌────┘        │        └────┐
         │             │             │
    ┌────┴────┐  ┌────┴────┐  ┌────┴────┐
    │ MySQL   │  │PostgreSQL│  │ Oracle  │     ← "플러그"
    │ Plugin  │  │ Plugin   │  │ Plugin  │
    └─────────┘  └──────────┘  └─────────┘
```

### Eclipse Equinox

**Equinox = Eclipse의 OSGi 구현체**

```
OSGi 스펙 (표준)
    ↓
    구현체들:
    ├── Eclipse Equinox  ← DBeaver가 사용
    ├── Apache Felix
    └── Knopflerfish
```

### 동적 로딩 / Hot Deploy

```java
// OSGi 덕분에 가능한 것들

// 1. 런타임에 플러그인 설치
bundleContext.installBundle("file:/path/to/new-db-plugin.jar");

// 2. 런타임에 플러그인 시작
bundle.start();

// 3. 런타임에 플러그인 업데이트
bundle.update();

// 4. 런타임에 플러그인 제거
bundle.uninstall();

// → DBeaver를 재시작하지 않고 새 DB 드라이버 추가 가능!
```

---

## 5. SWT와 UI 기술

### UI 기술 비교

```
Java UI 기술 계보:

AWT (1995) ──→ Swing (1998) ──→ JavaFX (2014)
    │
    └──→ SWT (2001) ──→ JFace
                           ↓
                       Eclipse RCP
                           ↓
                        DBeaver
```

### SWT vs Swing

| 특성 | SWT | Swing |
|------|-----|-------|
| **렌더링** | OS 네이티브 위젯 사용 | Java로 직접 그림 |
| **룩앤필** | OS와 동일 | 어디서나 동일 (또는 커스텀) |
| **성능** | 빠름 (네이티브 호출) | 상대적으로 느림 |
| **메모리** | OS 위젯 직접 사용 | JVM 내에서 관리 |
| **이식성** | 플랫폼별 바이너리 필요 | 어디서나 동일 JAR |
| **사용처** | Eclipse, DBeaver | IntelliJ IDEA |

```
SWT (Standard Widget Toolkit)

Mac에서 실행:
┌─────────────────────┐
│  macOS 네이티브     │
│  ┌─────────────────┐│
│  │ Cocoa 버튼      ││  ← macOS 기본 버튼
│  │ [  확인  ]      ││
│  └─────────────────┘│
└─────────────────────┘

Windows에서 실행:
┌─────────────────────┐
│  Windows 네이티브   │
│  ┌─────────────────┐│
│  │ Win32 버튼      ││  ← Windows 기본 버튼
│  │ [  확인  ]      ││
│  └─────────────────┘│
└─────────────────────┘
```

### JFace

**JFace = SWT를 더 쉽게 사용하기 위한 래퍼**

```java
// 순수 SWT - 복잡함
Table table = new Table(parent, SWT.BORDER);
TableColumn col = new TableColumn(table, SWT.NONE);
col.setText("Name");
TableItem item = new TableItem(table, SWT.NONE);
item.setText("John");

// JFace TableViewer - 더 간단함
TableViewer viewer = new TableViewer(parent, SWT.BORDER);
viewer.setContentProvider(new ArrayContentProvider());
viewer.setLabelProvider(new MyLabelProvider());
viewer.setInput(myDataList);  // 데이터 바인딩!
```

```
SWT와 JFace 관계:

┌─────────────────────────────┐
│  JFace (고수준 API)         │
│  - Viewer, Dialog, Wizard   │
│  - 데이터 바인딩            │
│  - MVC 패턴 지원            │
└─────────────┬───────────────┘
              ↓
┌─────────────────────────────┐
│  SWT (저수준 위젯)          │
│  - Button, Text, Table      │
│  - Event 처리               │
│  - Layout                   │
└─────────────┬───────────────┘
              ↓
┌─────────────────────────────┐
│  OS 네이티브 (C/C++)        │
│  - Cocoa (Mac)              │
│  - GTK (Linux)              │
│  - Win32 (Windows)          │
└─────────────────────────────┘
```

---

## 6. SQL Dialect - 100개 DB 지원의 비밀

### 문제: DB마다 SQL이 다르다

```sql
-- 페이지네이션 예시

-- MySQL
SELECT * FROM users LIMIT 10 OFFSET 20;

-- Oracle (12c 이전)
SELECT * FROM (
  SELECT a.*, ROWNUM rnum FROM (
    SELECT * FROM users
  ) a WHERE ROWNUM <= 30
) WHERE rnum > 20;

-- SQL Server
SELECT * FROM users
ORDER BY id
OFFSET 20 ROWS FETCH NEXT 10 ROWS ONLY;

-- PostgreSQL
SELECT * FROM users LIMIT 10 OFFSET 20;
```

```sql
-- 식별자 인용 (Quote)

-- MySQL: 백틱 사용
SELECT `order`, `select` FROM `table`;

-- PostgreSQL/Oracle: 쌍따옴표 사용
SELECT "order", "select" FROM "table";

-- SQL Server: 대괄호 사용
SELECT [order], [select] FROM [table];
```

### 해결: SQLDialect 추상화

```
                    SQLDialect (인터페이스)
                           │
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
   MySQLDialect      PostgreDialect     OracleDialect
   - 백틱 사용        - "" 사용          - "" 사용
   - LIMIT/OFFSET    - LIMIT/OFFSET     - ROWNUM
   - # 주석 지원      - $$ 함수 지원     - /* */ 힌트
```

### SQLDialect 인터페이스 핵심 메서드

```java
// 위치: org.jkiss.dbeaver.model.sql.SQLDialect

public interface SQLDialect {

    // 식별자 인용
    String[][] getIdentifierQuoteStrings();
    // MySQL: {{"`", "`"}, {"\"", "\""}}
    // PostgreSQL: {{"\"", "\""}}

    // 문자열 이스케이프
    char getStringEscapeCharacter();
    // MySQL: '\\'
    // PostgreSQL: 서버 설정에 따라 다름

    // LIMIT/OFFSET 문법
    String getOffsetLimitQueryPart(int offset, int limit);
    // MySQL: "LIMIT 10 OFFSET 20"
    // Oracle: 완전히 다른 구조

    // 주석 문법
    String[] getSingleLineComments();
    // MySQL: {"-- ", "--\t", "#"}
    // PostgreSQL: {"--"}

    // 예약어 목록
    Collection<String> getReservedWords();

    // 내장 함수 목록
    Collection<String> getFunctions();

    // 데이터 타입 목록
    Collection<String> getDataTypes();
}
```

### 실제 구현 비교

```java
// MySQLDialect.java (라인 64-67)
public static final String[][] MYSQL_QUOTE_STRINGS = {
    {"`", "`"},      // 기본: 백틱
    {"\"", "\""},    // ANSI 모드
};

// MySQLDialect.java (라인 312-313)
public String[] getSingleLineComments() {
    return new String[] { "-- ", "--\t", "#" };  // # 주석 지원!
}

// PostgreDialect.java (라인 60-62)
private static final String[][] PG_STRING_QUOTES = {
    {"'", "'"}
};

// PostgreDialect.java (라인 1106-1124)
// PostgreSQL만의 특수 문법 파서 규칙
public TPRule[] extendRules(...) {
    return new TPRule[] {
        new SQLDollarQuoteRule(...),    // $$ ... $$ 함수 본문
        new PostgreEscapeStringRule()   // E'...' 이스케이프 문자열
    };
}
```

### 새 DB 지원 추가하는 법

```java
// 1. SQLDialect 구현
public class NewDBDialect extends JDBCSQLDialect {

    public NewDBDialect() {
        super("NewDB", "newdb");
    }

    @Override
    public String[][] getIdentifierQuoteStrings() {
        return new String[][] {{"[", "]"}};  // 예: SQL Server 스타일
    }

    @Override
    public String getOffsetLimitQueryPart(int offset, int limit) {
        return "SKIP " + offset + " FIRST " + limit;  // 가상의 문법
    }
}

// 2. plugin.xml에 등록
<extension point="org.jkiss.dbeaver.sqlDialect">
    <dialect
        id="newdb"
        class="org.jkiss.dbeaver.ext.newdb.NewDBDialect"/>
</extension>
```

---

## 7. 라이선스와 오픈소스

### Apache License 2.0

**DBeaver CE의 라이선스**

```
주요 특징:
✅ 상업적 사용 가능
✅ 수정 가능
✅ 배포 가능
✅ 특허권 부여
✅ 서브라이선스 가능

의무:
📋 라이선스 고지 포함
📋 변경 사항 표시
📋 NOTICE 파일 유지

제한 없음:
❌ 소스 공개 의무 없음 (GPL과 다름!)
❌ 동일 라이선스 강제 없음
```

### Permissive vs Copyleft

```
Permissive 라이선스                 Copyleft 라이선스
(허용적)                           (전염성)
────────────────                   ─────────────────
- Apache 2.0 ← DBeaver             - GPL
- MIT                              - LGPL
- BSD                              - AGPL

특징:                              특징:
- 소스 공개 의무 없음              - 수정본도 같은 라이선스 적용
- 상업적 사용 자유                 - 소스 공개 의무
- 기업 친화적                      - "바이러스성" 라이선스
```

### 아파치 소프트웨어 재단 (ASF)

```
Apache Software Foundation이 관리하는 프로젝트들:
- Apache HTTP Server (웹서버)
- Apache Kafka (메시징)
- Apache Spark (빅데이터)
- Apache Maven (빌드 도구)
- Apache POI (Office 파일 처리) ← DBeaver에서 사용

DBeaver는 ASF 프로젝트는 아니지만, Apache 2.0 라이선스 사용
```

---

## 8. P2 - 배포와 업데이트

### P2란?

**P2 = Eclipse Provisioning Platform (프로비저닝 플랫폼)**

Eclipse 기반 애플리케이션의 **설치, 업데이트, 의존성 관리** 시스템

```
"앱스토어 같은 것"

사용자가 DBeaver 실행 → 업데이트 확인 → P2 저장소에서 다운로드 → 설치
```

### P2 저장소 구조

```
repository/
├── content.jar          ← 설치 가능한 유닛 목록 (메타데이터)
├── artifacts.jar        ← 실제 파일 위치 정보
├── p2.index            ← 저장소 인덱스
├── features/           ← Feature JAR 파일들
│   ├── org.jkiss.dbeaver.feature_1.0.0.jar
│   └── ...
└── plugins/            ← Plugin JAR 파일들
    ├── org.jkiss.dbeaver.core_1.0.0.jar
    └── ...
```

### DBeaver의 P2 사용

```
DBeaver 빌드 시:
1. Maven + Tycho로 빌드
2. P2 저장소 생성 (product/repositories/)
3. 사용자에게 배포

사용자 업데이트 시:
1. Help → Check for Updates
2. P2가 저장소에서 새 버전 확인
3. 변경된 플러그인만 다운로드
4. 자동 설치 및 재시작
```

```
product/
└── repositories/
    └── org.jkiss.dbeaver.repository/
        └── target/
            └── repository/        ← P2 저장소
                ├── content.jar
                ├── artifacts.jar
                └── ...
```

### Provisioning 개념

```
프로비저닝 = "미리 준비해두는 것"

서버 프로비저닝:                    소프트웨어 프로비저닝:
- 서버 세팅                        - 앱 설치
- OS 설치                          - 의존성 해결
- 환경 구성                        - 업데이트
                                   - 롤백

P2 = Eclipse 생태계의 소프트웨어 프로비저닝 시스템
```

---

## 9. 유사 도구 비교

### DB 클라이언트 도구들

| 도구 | 개발사 | 라이선스 | 기술 스택 | 특징 |
|------|--------|----------|-----------|------|
| **DBeaver CE** | DBeaver Corp | Apache 2.0 | Java, Eclipse RCP, SWT | 100+ DB 지원, 무료 |
| **DBeaver Pro** | DBeaver Corp | 상용 | 위와 동일 | NoSQL, 기술지원 |
| **DataGrip** | JetBrains | 상용 | Java, IntelliJ Platform, Swing | 강력한 IDE 기능 |
| **MySQL Workbench** | Oracle | GPL | C++, GTK | MySQL 특화 |
| **pgAdmin** | PostgreSQL Community | PostgreSQL License | Python, Qt/Web | PostgreSQL 특화 |
| **Oracle SQL Developer** | Oracle | OTN License | Java, Swing | Oracle 특화 |
| **Azure Data Studio** | Microsoft | MIT | TypeScript, Electron | SQL Server 특화 |

### 아키텍처 비교

```
DBeaver                          DataGrip
────────                         ────────
Eclipse RCP                      IntelliJ Platform
    │                                │
   SWT (네이티브)                  Swing (Java 렌더링)
    │                                │
    └─→ OS 네이티브 위젯            └─→ JVM에서 직접 그림

장점:                            장점:
- 네이티브 룩앤필                - 일관된 크로스 플랫폼 경험
- 빠른 렌더링                    - JetBrains 에코시스템
- 작은 메모리                    - 강력한 코드 분석

단점:                            단점:
- 플랫폼별 빌드 필요             - 상대적으로 무거움
- SWT 학습 곡선                  - 유료
```

### IntelliJ Database Tools vs DBeaver

```
IntelliJ IDEA (Ultimate)에 포함된 DB 도구

공통점:
- 다중 DB 지원
- SQL 자동완성
- ER 다이어그램

차이점:
┌─────────────────────┬─────────────────────┐
│ IntelliJ DB Tools   │ DBeaver             │
├─────────────────────┼─────────────────────┤
│ IDE 통합 (한 창에서)│ 독립 애플리케이션    │
│ 코드와 DB 동시 작업 │ DB 작업에 집중       │
│ 유료 (Ultimate)     │ 무료 (CE)           │
│ JetBrains 스타일    │ Eclipse 스타일      │
└─────────────────────┴─────────────────────┘
```

---

## 부록: 용어 빠른 참조

| 용어 | 한 줄 설명 |
|------|-----------|
| **JDBC** | Java에서 DB 접근하는 표준 API |
| **JDBC Driver** | DB 벤더가 만든 JDBC 구현체 (JAR 파일) |
| **Eclipse RCP** | 데스크톱 앱 만드는 프레임워크 |
| **OSGi** | Java 모듈/플러그인 시스템 표준 |
| **Bundle** | OSGi에서 플러그인을 부르는 이름 |
| **Equinox** | Eclipse가 사용하는 OSGi 구현체 |
| **SWT** | Eclipse의 네이티브 UI 툴킷 |
| **JFace** | SWT를 쉽게 쓰게 해주는 래퍼 |
| **Extension Point** | 플러그인이 꽂히는 소켓 |
| **Extension** | Extension Point에 꽂히는 플러그 |
| **SQLDialect** | DB별 SQL 문법 차이를 추상화한 인터페이스 |
| **P2** | Eclipse 앱의 설치/업데이트 시스템 |
| **MANIFEST.MF** | OSGi 번들 정의 파일 |
| **plugin.xml** | Eclipse 확장 포인트 정의 파일 |

---

## 학습 순서 추천

```
1단계: JDBC 이해
   └─→ Java로 DB 연결하는 기본 원리

2단계: DBeaver가 JDBC를 어떻게 활용하는지
   └─→ JDBCDataSource, DriverManager

3단계: Eclipse RCP 기본
   └─→ Workbench, View, Editor 개념

4단계: OSGi 플러그인 시스템
   └─→ Bundle, MANIFEST.MF, Extension Point

5단계: SQLDialect 분석
   └─→ 다중 DB 지원의 핵심 원리

6단계: 실제 코드 읽기
   └─→ PostgreSQL 플러그인 분석
```

---

*마지막 업데이트: 2024년*
