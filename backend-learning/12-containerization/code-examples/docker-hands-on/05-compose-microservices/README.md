# Docker Compose 마이크로서비스 실습

## 🎯 학습 목표

- Docker Compose로 여러 서비스 연동
- 마이크로서비스 아키텍처 이해
- 서비스 간 통신
- 데이터 영속화
- 네트워크 구성

## 🏗️ 아키텍처

```
┌──────────────────────────────────────────────────┐
│                   Nginx                          │
│            (Reverse Proxy)                       │
│              Port: 80                            │
└───────────┬──────────────────────────────────────┘
            │
    ┌───────┼───────┬───────────┬────────┐
    │               │           │        │
    ▼               ▼           ▼        ▼
┌────────┐    ┌──────────┐ ┌────────┐ ┌────────┐
│  Web   │    │   API    │ │ Auth   │ │ Admin  │
│ Frontend│   │ Backend  │ │Service │ │ Panel  │
│ (React)│    │ (Node.js)│ │(Node.js│ │(React) │
│        │    │          │ │        │ │        │
└────────┘    └─────┬────┘ └───┬────┘ └────────┘
                    │          │
              ┌─────┼──────────┤
              │     │          │
              ▼     ▼          ▼
         ┌─────────┐      ┌─────────┐
         │PostgreSQL│      │  Redis  │
         │         │      │ (Cache) │
         └─────────┘      └─────────┘
```

## 📁 프로젝트 구조

```
05-compose-microservices/
├── README.md
├── docker-compose.yml
├── docker-compose.dev.yml
├── docker-compose.prod.yml
├── .env.example
├── nginx/
│   ├── Dockerfile
│   └── nginx.conf
├── api/
│   ├── Dockerfile
│   ├── package.json
│   └── server.js
├── auth/
│   ├── Dockerfile
│   ├── package.json
│   └── server.js
├── web/
│   ├── Dockerfile
│   ├── package.json
│   ├── public/
│   └── src/
└── scripts/
    ├── init-db.sql
    └── test.sh
```

## 📝 Step 1: 환경 변수 설정

### .env.example
```bash
# 데이터베이스
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123
POSTGRES_DB=microservices
DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/microservices

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# JWT
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRY=24h

# API
API_PORT=3001
AUTH_PORT=3002

# 환경
NODE_ENV=development
