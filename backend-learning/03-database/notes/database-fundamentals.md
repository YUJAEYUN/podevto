# 데이터베이스 기초 개념

## 데이터베이스란?

**데이터베이스(Database)** 는 체계적으로 구조화된 데이터의 집합입니다. 데이터를 효율적으로 저장, 검색, 수정, 삭제할 수 있도록 관리하는 시스템입니다.

```
파일 시스템 vs 데이터베이스
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📁 파일 시스템 (users.txt)
┌────────────────────────────────┐
│ John,30,john@example.com       │
│ Jane,25,jane@example.com       │
└────────────────────────────────┘
문제점:
✗ 중복 데이터 관리 어려움
✗ 동시 접근 제어 불가
✗ 데이터 무결성 보장 안 됨
✗ 복잡한 검색 어려움

🗄️ 데이터베이스
┌────────────────────────────────┐
│ users 테이블                    │
├──────┬─────┬──────────────────┤
│ id   │ name│ email            │
├──────┼─────┼──────────────────┤
│ 1    │ John│ john@example.com │
│ 2    │ Jane│ jane@example.com │
└──────┴─────┴──────────────────┘
장점:
✓ 데이터 무결성 보장
✓ 동시성 제어
✓ 트랜잭션 지원
✓ 효율적인 검색
✓ 백업 및 복구
```

---

## 데이터베이스 관리 시스템 (DBMS)

**DBMS (Database Management System)** 는 데이터베이스를 관리하는 소프트웨어입니다.

```
애플리케이션
     ↓
  DBMS (PostgreSQL, MySQL 등)
     ↓
데이터베이스 (실제 데이터 파일)
```

### 주요 DBMS

```
관계형 DBMS (RDBMS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- PostgreSQL
- MySQL / MariaDB
- Oracle
- MS SQL Server
- SQLite

NoSQL DBMS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- MongoDB (Document)
- Redis (Key-Value)
- Cassandra (Column)
- Neo4j (Graph)
```

---

## 데이터베이스 유형

### 1. 관계형 데이터베이스 (RDBMS)

데이터를 테이블(표) 형태로 저장하고, 테이블 간 관계로 연결합니다.

```
users 테이블
┌────┬──────┬─────────────────┐
│ id │ name │ email           │
├────┼──────┼─────────────────┤
│ 1  │ John │ john@example.com│
│ 2  │ Jane │ jane@example.com│
└────┴──────┴─────────────────┘

orders 테이블
┌────┬─────────┬────────┬────────┐
│ id │ user_id │ product│ amount │
├────┼─────────┼────────┼────────┤
│ 1  │ 1       │ Laptop │ 1000   │
│ 2  │ 1       │ Mouse  │ 20     │
│ 3  │ 2       │ Phone  │ 800    │
└────┴─────────┴────────┴────────┘
         ↑
         └─ users.id와 연결 (Foreign Key)
```

**특징**:
- SQL (Structured Query Language) 사용
- 스키마 정의 필수
- 데이터 무결성 강력
- ACID 속성 보장

**언제 사용?**
- 데이터 간 관계가 중요한 경우
- 트랜잭션이 중요한 경우 (금융, 결제)
- 복잡한 쿼리가 필요한 경우

### 2. NoSQL 데이터베이스

관계형 모델을 따르지 않는 데이터베이스입니다.

#### Document DB (MongoDB)
```json
{
  "_id": "507f1f77bcf86cd799439011",
  "name": "John",
  "email": "john@example.com",
  "orders": [
    { "product": "Laptop", "amount": 1000 },
    { "product": "Mouse", "amount": 20 }
  ]
}
```

#### Key-Value (Redis)
```
key: "user:1"
value: '{"name":"John","email":"john@example.com"}'

key: "session:abc123"
value: '{"userId":1,"expires":1234567890}'
```

**특징**:
- 유연한 스키마
- 수평 확장 용이
- 빠른 읽기/쓰기
- 최종 일관성 (Eventual Consistency)

**언제 사용?**
- 대용량 데이터 처리
- 빠른 응답 속도가 중요한 경우
- 스키마가 자주 변경되는 경우

---

## 핵심 개념

### 1. 테이블 (Table)

데이터를 저장하는 기본 단위입니다.

```
테이블 구조
┌─────────────────────────────────────┐
│          users (테이블 이름)        │
├──────┬──────┬──────────────────────┤
│ id   │ name │ email            │ age│ ← 컬럼 (Column)
├──────┼──────┼──────────────────┼────┤
│ 1    │ John │ john@example.com │ 30 │ ← 행/레코드 (Row/Record)
│ 2    │ Jane │ jane@example.com │ 25 │
│ 3    │ Bob  │ bob@example.com  │ 35 │
└──────┴──────┴──────────────────┴────┘
```

### 2. 스키마 (Schema)

데이터베이스의 구조와 제약 조건을 정의합니다.

```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,  -- 기본 키
    name VARCHAR(100) NOT NULL,         -- 필수 필드
    email VARCHAR(255) UNIQUE NOT NULL, -- 고유값, 필수
    age INT CHECK (age >= 0),           -- 0 이상만 허용
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 3. 키 (Key)

#### 기본 키 (Primary Key)
```
각 레코드를 고유하게 식별하는 키

users 테이블
┌────┬──────┬─────────────────┐
│ id │ name │ email           │
└─┬──┴──────┴─────────────────┘
  └─ Primary Key (중복 불가, NULL 불가)
```

#### 외래 키 (Foreign Key)
```
다른 테이블의 기본 키를 참조하는 키

orders 테이블
┌────┬─────────┬────────┐
│ id │ user_id │ amount │
└────┴────┬────┴────────┘
          │
          └─→ users.id를 참조 (Foreign Key)
```

---

## CRUD 작업

데이터베이스의 기본 작업은 **CRUD** 입니다.

```
C - Create  (생성)   → INSERT
R - Read    (읽기)   → SELECT
U - Update  (수정)   → UPDATE
D - Delete  (삭제)   → DELETE
```

### 예시 (SQL)

```sql
-- CREATE (생성)
INSERT INTO users (name, email, age)
VALUES ('John', 'john@example.com', 30);

-- READ (읽기)
SELECT * FROM users WHERE age > 25;

-- UPDATE (수정)
UPDATE users
SET age = 31
WHERE id = 1;

-- DELETE (삭제)
DELETE FROM users WHERE id = 1;
```

---

## 데이터 타입

### 주요 데이터 타입

| 타입 | 설명 | 예시 | 사용 예 |
|------|------|------|---------|
| **INT** | 정수 | 1, 100, -5 | 나이, 수량 |
| **VARCHAR(n)** | 가변 길이 문자열 | "John" | 이름, 이메일 |
| **TEXT** | 긴 문자열 | 긴 본문 | 블로그 포스트 |
| **DATE** | 날짜 | 2024-01-01 | 생년월일 |
| **TIMESTAMP** | 날짜+시간 | 2024-01-01 10:30:00 | 생성 시간 |
| **BOOLEAN** | 참/거짓 | TRUE, FALSE | 활성 여부 |
| **DECIMAL(p,s)** | 고정 소수점 | 99.99 | 가격, 금액 |

### 타입 선택 가이드

```
이름, 이메일 → VARCHAR(255)
짧은 코드 (2~10자) → CHAR(n) 또는 VARCHAR(10)
긴 텍스트 → TEXT
나이, 수량 → INT
가격, 금액 → DECIMAL(10, 2)
날짜 → DATE
생성/수정 시간 → TIMESTAMP
활성 여부 → BOOLEAN
```

---

## 데이터베이스 연결

### Node.js에서 PostgreSQL 연결

```javascript
const { Pool } = require('pg');

const pool = new Pool({
  host: 'localhost',
  port: 5432,
  user: 'postgres',
  password: 'password',
  database: 'mydb'
});

// 쿼리 실행
async function getUsers() {
  const result = await pool.query('SELECT * FROM users');
  return result.rows;
}
```

### Python에서 PostgreSQL 연결

```python
import psycopg2

# 연결
conn = psycopg2.connect(
    host="localhost",
    port=5432,
    user="postgres",
    password="password",
    database="mydb"
)

# 쿼리 실행
cursor = conn.cursor()
cursor.execute("SELECT * FROM users")
users = cursor.fetchall()
```

---

## 백엔드에서 데이터베이스 역할

```
┌─────────────────────────────────────┐
│        클라이언트 (브라우저)         │
└──────────────┬──────────────────────┘
               ↓ HTTP 요청
┌─────────────────────────────────────┐
│      백엔드 API 서버 (Express)       │
├─────────────────────────────────────┤
│ GET /api/users                      │
│   → 데이터베이스에서 users 조회      │
│                                     │
│ POST /api/users                     │
│   → 데이터베이스에 user 생성         │
└──────────────┬──────────────────────┘
               ↓ SQL 쿼리
┌─────────────────────────────────────┐
│         데이터베이스 (PostgreSQL)    │
├─────────────────────────────────────┤
│ users 테이블                         │
│ orders 테이블                        │
│ products 테이블                      │
└─────────────────────────────────────┘
```

### 전형적인 흐름

```javascript
// Express API 예시
app.get('/api/users', async (req, res) => {
  try {
    // 1. 데이터베이스 쿼리
    const result = await pool.query('SELECT * FROM users');

    // 2. 결과를 JSON으로 반환
    res.json({
      success: true,
      data: result.rows
    });
  } catch (error) {
    res.status(500).json({
      success: false,
      error: error.message
    });
  }
});
```

---

## 데이터베이스 설계 원칙

### 1. 정규화 (Normalization)

데이터 중복을 최소화하고 무결성을 보장합니다.

```
나쁜 설계 (중복):
┌────┬──────┬─────────┬──────────┬──────────┐
│ id │ name │ product1│ product2 │ product3 │
├────┼──────┼─────────┼──────────┼──────────┤
│ 1  │ John │ Laptop  │ Mouse    │ NULL     │
└────┴──────┴─────────┴──────────┴──────────┘

좋은 설계 (정규화):
users
┌────┬──────┐
│ id │ name │
├────┼──────┤
│ 1  │ John │
└────┴──────┘

orders
┌────┬─────────┬─────────┐
│ id │ user_id │ product │
├────┼─────────┼─────────┤
│ 1  │ 1       │ Laptop  │
│ 2  │ 1       │ Mouse   │
└────┴─────────┴─────────┘
```

### 2. 인덱스 (Index)

검색 속도를 향상시키는 데이터 구조입니다.

```sql
-- 이메일로 자주 검색하는 경우
CREATE INDEX idx_users_email ON users(email);

-- 쿼리 속도 향상
SELECT * FROM users WHERE email = 'john@example.com';
-- 인덱스 없음: 1000ms
-- 인덱스 있음: 10ms
```

### 3. 제약 조건 (Constraints)

데이터 무결성을 보장합니다.

```sql
CREATE TABLE users (
    id INT PRIMARY KEY,              -- 기본 키 제약
    email VARCHAR(255) UNIQUE,       -- 고유 제약
    name VARCHAR(100) NOT NULL,      -- NOT NULL 제약
    age INT CHECK (age >= 18)        -- CHECK 제약
);
```

---

## 일반적인 실수

### 1. SELECT * 남용

```sql
-- ❌ 나쁜 예
SELECT * FROM users;

-- ✅ 좋은 예
SELECT id, name, email FROM users;
```

**이유**: 필요 없는 컬럼까지 가져와서 성능 낭비

### 2. 인덱스 없는 검색

```sql
-- email로 자주 검색하는데 인덱스가 없음
SELECT * FROM users WHERE email = 'john@example.com';
-- 매우 느림!

-- 해결: 인덱스 생성
CREATE INDEX idx_users_email ON users(email);
```

### 3. SQL Injection 취약점

```javascript
// ❌ 위험! SQL Injection 가능
const query = `SELECT * FROM users WHERE email = '${userInput}'`;

// ✅ 안전: 파라미터 바인딩 사용
const query = 'SELECT * FROM users WHERE email = $1';
await pool.query(query, [userInput]);
```

---

## 추가 학습 자료

- [Database Fundamentals | Khan Academy](https://www.khanacademy.org/computing/computer-science/sql)
- [SQL Tutorial | W3Schools](https://www.w3schools.com/sql/)
- [PostgreSQL Tutorial](https://www.postgresqltutorial.com/)

---

## 다음 학습

- [관계형 vs NoSQL](relational-vs-nosql.md)
- [SQL 기초](sql-basics.md)
- [PostgreSQL 완벽 가이드](postgresql-guide.md)

---

*Last updated: 2026-01-05*
