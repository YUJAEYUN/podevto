# HTTPS / SSL / TLS 완벽 가이드

> 웹 통신 암호화의 모든 것: 인증서, 핸드셰이크, 암호화 흐름

## 개요

**HTTPS**는 HTTP에 **TLS(Transport Layer Security)** 암호화 계층을 추가한 프로토콜입니다. 데이터를 암호화하여 중간자 공격(MITM)으로부터 보호합니다.

### 핵심 아이디어

```
"HTTP는 평문, HTTPS는 암호화된 통신"

┌─────────────────────────────────────────────────────────────────────┐
│                       HTTP vs HTTPS 비교                             │
│                                                                     │
│   HTTP (위험):                                                      │
│   ┌──────────┐    평문 전송    ┌──────────┐                        │
│   │ 브라우저  │ ─────────────▶ │  서버     │                        │
│   └──────────┘  ID: admin     └──────────┘                        │
│                 PW: 1234                                           │
│                    ↑                                               │
│               해커가 패킷 훔쳐봄 → 내용 다 보임!                     │
│                                                                     │
│   HTTPS (안전):                                                     │
│   ┌──────────┐    암호화 전송   ┌──────────┐                        │
│   │ 브라우저  │ ─────────────▶ │  서버     │                        │
│   └──────────┘  x#@!%&*...    └──────────┘                        │
│                    ↑                                               │
│               해커가 패킷 훔쳐도 → 해독 불가!                        │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## SSL vs TLS

```
역사:
─────────────────────────────────────────
SSL 1.0 (1994) → 공개 안 됨 (보안 결함)
SSL 2.0 (1995) → 취약점 발견
SSL 3.0 (1996) → 취약점 발견 (POODLE 공격)
TLS 1.0 (1999) → SSL 3.0 기반 개선
TLS 1.1 (2006) → 보안 강화
TLS 1.2 (2008) → 현재 널리 사용 ✅
TLS 1.3 (2018) → 최신, 더 빠르고 안전 ✅
```

| 구분 | SSL | TLS |
|------|-----|-----|
| **상태** | 폐기됨 (사용 금지) | 현재 표준 |
| **버전** | 2.0, 3.0 | 1.2, 1.3 |
| **보안** | 취약점 多 | 안전 |

> 💡 실무에서 "SSL 인증서"라고 말해도 실제로는 **TLS**를 사용합니다.
> 용어만 관습적으로 SSL이라고 부르는 것!

---

## HTTPS가 제공하는 3가지 보안

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      HTTPS의 3가지 보안 요소                              │
│                                                                         │
│   ┌───────────────────┐                                                │
│   │  1. 기밀성         │  데이터 암호화                                  │
│   │  (Confidentiality)│  → 중간에서 패킷을 훔쳐도 내용 해독 불가         │
│   └───────────────────┘                                                │
│                                                                         │
│   ┌───────────────────┐                                                │
│   │  2. 무결성         │  데이터 변조 방지                               │
│   │  (Integrity)      │  → 중간에서 데이터 변조 시 MAC 검증에서 탐지     │
│   └───────────────────┘                                                │
│                                                                         │
│   ┌───────────────────┐                                                │
│   │  3. 인증           │  서버 신원 확인                                 │
│   │  (Authentication) │  → "이 서버가 진짜 google.com인가?" 확인        │
│   └───────────────────┘                                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 암호화 기초 개념

### 대칭키 vs 비대칭키

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        암호화 방식 비교                                   │
│                                                                         │
│   대칭키 암호화 (Symmetric)                                              │
│   ─────────────────────────────────────────                             │
│   • 암호화/복호화에 같은 키 사용                                         │
│   • 빠름 (대용량 데이터에 적합)                                          │
│   • 예: AES, ChaCha20                                                   │
│                                                                         │
│   ┌─────┐      같은 키       ┌─────┐                                   │
│   │평문  │ ──── 🔑 ────────▶ │암호문│                                   │
│   └─────┘                    └─────┘                                   │
│                같은 키                                                   │
│   ┌─────┐ ◀─── 🔑 ──────── ┌─────┐                                    │
│   │평문  │                  │암호문│                                   │
│   └─────┘                    └─────┘                                   │
│                                                                         │
│   문제: 키를 어떻게 안전하게 전달하지? 🤔                                │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   비대칭키 암호화 (Asymmetric)                                           │
│   ─────────────────────────────────────────                             │
│   • 공개키/개인키 쌍 사용                                                │
│   • 느림 (키 교환에 적합)                                                │
│   • 예: RSA, ECDHE                                                      │
│                                                                         │
│   ┌─────┐     공개키 🔓      ┌─────┐                                   │
│   │평문  │ ────────────────▶ │암호문│   ← 누구나 암호화 가능            │
│   └─────┘                    └─────┘                                   │
│                개인키 🔐                                                 │
│   ┌─────┐ ◀──────────────── ┌─────┐                                   │
│   │평문  │                  │암호문│   ← 개인키 소유자만 복호화          │
│   └─────┘                    └─────┘                                   │
│                                                                         │
│   핵심: 공개키로 암호화 → 개인키로만 복호화                              │
│        공개키는 공개해도 OK, 개인키만 비밀로!                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### TLS에서의 사용

```
TLS는 두 방식을 조합해서 사용:
─────────────────────────────────────────

1. 비대칭키 (RSA, ECDHE)
   → 세션키를 안전하게 교환하는 용도
   → 핸드셰이크 과정에서 사용

2. 대칭키 (AES, ChaCha20)
   → 실제 데이터(ID, PW, JSON 등)를 암호화하는 용도
   → 핸드셰이크 이후 모든 통신에 사용

왜 이렇게?
→ 비대칭키는 안전하지만 느림
→ 대칭키는 빠르지만 키 전달이 문제
→ 비대칭키로 대칭키(세션키)를 안전하게 교환하고,
   이후에는 빠른 대칭키로 통신!
```

---

## 인증서와 CA

### 인증서 (Certificate)란?

```
인증서 = 서버의 신분증
─────────────────────────────────────────

┌─────────────────────────────────────────────────────────┐
│                    SSL/TLS 인증서                        │
├─────────────────────────────────────────────────────────┤
│  도메인: api.example.com                                │
│  발급자: Let's Encrypt (CA)                             │
│  유효기간: 2025-01-01 ~ 2025-03-31                      │
│  서버 공개키: MIIBIjANBgkqh... (RSA 2048bit)            │
│  서명: CA가 개인키로 서명한 값                           │
└─────────────────────────────────────────────────────────┘

인증서에 포함된 정보:
• 도메인 이름 (CN: Common Name)
• 서버의 공개키 ← 중요!
• 발급 기관 (CA)
• 유효 기간
• CA의 디지털 서명 ← 위조 방지
```

### CA (Certificate Authority)

```
CA = 인증서를 발급하는 신뢰할 수 있는 기관
─────────────────────────────────────────

신뢰 체인 (Chain of Trust):
┌─────────────────┐
│   Root CA       │  ← 브라우저에 미리 내장된 최상위 기관
│ (DigiCert 등)   │     (약 100~150개)
└────────┬────────┘
         │ 서명
         ▼
┌─────────────────┐
│ Intermediate CA │  ← 중간 인증 기관
│                 │
└────────┬────────┘
         │ 서명
         ▼
┌─────────────────┐
│  서버 인증서     │  ← 우리 서버의 인증서
│ api.example.com │
└─────────────────┘

브라우저 검증 과정:
1. 서버 인증서의 서명을 Intermediate CA 공개키로 검증
2. Intermediate CA 인증서의 서명을 Root CA 공개키로 검증
3. Root CA는 브라우저에 미리 신뢰됨 → 체인 검증 완료!
```

### 인증서 발급 옵션

| 종류 | 가격 | 검증 수준 | 용도 |
|------|------|-----------|------|
| **DV (Domain Validation)** | 무료~저렴 | 도메인 소유만 확인 | 개인, 소규모 |
| **OV (Organization Validation)** | 유료 | 조직 실체 확인 | 기업 |
| **EV (Extended Validation)** | 고가 | 철저한 조직 검증 | 금융, 대기업 |

> 💡 **Let's Encrypt**: 무료 DV 인증서 발급, 자동 갱신 가능 (90일 유효)

---

## TLS 핸드셰이크 (상세)

### 전체 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      TLS 1.2 핸드셰이크 과정                              │
│                                                                         │
│   브라우저 (클라이언트)                        서버 (Nginx)              │
│         │                                         │                     │
│         │ ──── 1. ClientHello ──────────────────▶ │                     │
│         │      "TLS 연결하고 싶어요"                │                     │
│         │      지원 암호화 목록, 랜덤값              │                     │
│         │                                         │                     │
│         │ ◀─── 2. ServerHello ────────────────── │                     │
│         │      "이 암호화 방식 쓰자"                │                     │
│         │      선택된 암호화, 랜덤값                │                     │
│         │                                         │                     │
│         │ ◀─── 3. Certificate ───────────────── │                     │
│         │      "내 인증서야 (공개키 포함)"          │                     │
│         │                                         │                     │
│         │ ◀─── 4. ServerHelloDone ───────────── │                     │
│         │      "서버 준비 완료"                    │                     │
│         │                                         │                     │
│    [인증서 검증]                                   │                     │
│    CA 체인으로 신뢰성 확인                          │                     │
│         │                                         │                     │
│         │ ──── 5. ClientKeyExchange ───────────▶ │                     │
│         │      Pre-Master Secret                  │                     │
│         │      (서버 공개키로 암호화)               │                     │
│         │                                         │                     │
│    [세션키 계산]                              [세션키 계산]              │
│    랜덤값들 + Pre-Master                     랜덤값들 + Pre-Master       │
│    = Master Secret                           = Master Secret            │
│    = 세션키 (대칭키)                          = 세션키 (대칭키)           │
│         │                                         │                     │
│         │ ──── 6. ChangeCipherSpec ────────────▶ │                     │
│         │ ──── 7. Finished (암호화됨) ──────────▶ │                     │
│         │                                         │                     │
│         │ ◀─── 8. ChangeCipherSpec ───────────── │                     │
│         │ ◀─── 9. Finished (암호화됨) ──────────  │                     │
│         │                                         │                     │
│    ════════════════════════════════════════════════                    │
│         │                                         │                     │
│         │ ◀════ 이후 모든 통신은 세션키로 암호화 ════▶ │                  │
│         │      (ID, PW, JSON 등)                  │                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 키 정리

```
TLS에서 사용되는 3가지 키:
─────────────────────────────────────────

1. CA 공개키 (브라우저에 내장)
   └── 용도: 인증서가 진짜 CA가 서명한 것인지 검증
   └── 위치: 브라우저에 미리 설치됨

2. 서버 공개키 (인증서에 포함)
   └── 용도: Pre-Master Secret을 암호화해서 서버에 전달
   └── 위치: 인증서 안에 포함되어 전송됨
   └── 특징: 훔쳐가도 괜찮음 (암호화만 가능, 복호화 불가)

3. 서버 개인키 (서버에만 존재)
   └── 용도: Pre-Master Secret 복호화
   └── 위치: 서버 파일시스템 (privkey.pem)
   └── 특징: 절대 유출되면 안 됨! 🚨

4. 세션키 = 대칭키 (핸드셰이크 후 양쪽에 존재)
   └── 용도: 실제 데이터 (ID, PW, JSON) 암호화/복호화
   └── 위치: 브라우저 + 서버 메모리에만 존재
   └── 특징: 연결 종료 시 폐기, 매 세션마다 새로 생성
```

### 핵심 포인트

```
Q: 공개키를 해커가 훔치면?
A: 상관없음! 공개키로는 암호화만 가능.
   복호화하려면 개인키가 필요한데, 그건 서버에만 있음.

Q: 개인키를 해커가 훔치면?
A: 게임 오버! 🚨
   모든 과거/현재 세션을 복호화할 수 있음.
   → 즉시 인증서 폐기하고 새로 발급해야 함.

Q: 세션키는 어디에?
A: 브라우저와 서버 메모리에만 존재.
   네트워크로 전송되지 않음 (Pre-Master만 암호화되어 전송).
```

---

## SSL 종료 (SSL Termination)

### 개념

```
SSL 종료 = TLS 암호화/복호화가 끝나는 지점
─────────────────────────────────────────

일반적으로 Nginx가 SSL 종료점 역할:

외부 (인터넷)              내부 (서버 내부망)
     │                           │
     │   HTTPS (암호화)          │   HTTP (평문)
     │                           │
┌────▼────┐              ┌───────▼───────┐
│ 브라우저 │ ──HTTPS───▶ │    Nginx      │ ──HTTP──▶ WAS (NestJS)
└─────────┘   암호화     │  (SSL 종료)   │   평문    localhost:3000
                         │               │
                         │ 여기서 복호화! │
                         └───────────────┘
```

### 왜 Nginx에서 SSL 종료?

```
장점:
─────────────────────────────────────────
1. WAS 부담 감소
   - 암호화/복호화는 CPU 집약적 작업
   - Nginx가 전담하면 WAS는 비즈니스 로직에 집중

2. 인증서 관리 단순화
   - Nginx 한 곳에서만 인증서 관리
   - WAS가 여러 대여도 인증서는 Nginx에만

3. 내부망 보안 가정
   - 내부망은 안전하다고 가정 (방화벽, VPC 등)
   - 외부 공격만 막으면 됨

4. 유연한 라우팅
   - Nginx가 먼저 요청을 받아서 분석
   - 여러 WAS로 로드밸런싱 가능
```

### Nginx SSL 설정 예시

```nginx
server {
    listen 443 ssl http2;
    server_name api.example.com;

    # 인증서 경로
    ssl_certificate /etc/letsencrypt/live/api.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.example.com/privkey.pem;

    # TLS 버전 설정 (1.2, 1.3만 허용)
    ssl_protocols TLSv1.2 TLSv1.3;

    # 암호화 스위트
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers off;

    # HSTS (HTTP Strict Transport Security)
    add_header Strict-Transport-Security "max-age=63072000" always;

    # 백엔드로 프록시 (평문 HTTP)
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;  # HTTPS 정보 전달
    }
}

# HTTP → HTTPS 리다이렉트
server {
    listen 80;
    server_name api.example.com;
    return 301 https://$server_name$request_uri;
}
```

---

## 로그인 요청 암호화 흐름 예시

```
┌─────────────────────────────────────────────────────────────────────────┐
│               사용자가 로그인할 때 실제로 일어나는 일                      │
│                                                                         │
│  1. 사용자가 https://api.example.com 접속                               │
│                                                                         │
│  2. TLS 핸드셰이크 (위에서 설명한 과정)                                   │
│     → 브라우저와 서버가 세션키 공유                                      │
│                                                                         │
│  3. 사용자가 ID/PW 입력 후 로그인 버튼 클릭                               │
│                                                                         │
│  4. 브라우저가 요청 생성:                                                │
│     POST /api/login                                                     │
│     {"email": "user@test.com", "password": "mypassword123"}             │
│                                                                         │
│  5. 브라우저가 세션키(AES)로 암호화:                                     │
│     원본: {"email": "user@test.com", "password": "mypassword123"}       │
│     암호화 후: x#@!kL9$mN&*...qW2eR (해독 불가한 바이너리)               │
│                                                                         │
│  6. 암호화된 데이터가 인터넷 통과                                        │
│     → 해커가 패킷 캡처해도 x#@!kL9$mN&*... 만 보임                      │
│     → 세션키 없이는 복호화 불가                                          │
│                                                                         │
│  7. Nginx가 수신 → 세션키로 복호화                                       │
│     → {"email": "user@test.com", "password": "mypassword123"}           │
│                                                                         │
│  8. Nginx → NestJS로 평문 HTTP 전달 (내부망)                            │
│                                                                         │
│  9. NestJS가 로직 처리 (DB 조회, 토큰 발급 등)                           │
│                                                                         │
│  10. 응답도 동일하게 암호화되어 브라우저로 전송                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 피싱/위조 공격과 인증서

### 공격 시나리오

```
시나리오 1: 공개키 탈취
─────────────────────────────────────────
공격자: 서버 공개키를 훔쳤다!
결과: 아무 의미 없음 ❌
이유: 공개키는 원래 누구나 볼 수 있는 값
      암호화만 가능, 복호화 불가

시나리오 2: 피싱 사이트 (유사 도메인)
─────────────────────────────────────────
공격자: g00gle.com (0은 숫자) 도메인으로
        Let's Encrypt 인증서 발급받음
결과: 기술적으로 HTTPS는 유효! ⚠️
      → 브라우저는 "안전함" 표시
      → 사용자가 속아서 ID/PW 입력
대응: 사용자가 URL을 직접 확인해야 함
      (도메인 스펠링 체크)

시나리오 3: Self-Signed 인증서
─────────────────────────────────────────
공격자: CA 없이 직접 만든 인증서 사용
결과: 브라우저가 경고 표시! ✅
      "이 연결은 안전하지 않습니다"
대응: 사용자가 경고 무시하지 않으면 OK
```

### 인증서가 보호해주는 것 / 아닌 것

```
✅ 인증서가 보호:
─────────────────────────────────────────
• 이 서버가 "google.com"인지 확인 (인증)
• 중간자 공격으로 데이터 도청 방지 (기밀성)
• 데이터 변조 방지 (무결성)

❌ 인증서가 보호 못함:
─────────────────────────────────────────
• 피싱 사이트 (g00gle.com도 인증서 받을 수 있음)
• 사용자가 직접 입력한 정보의 안전성
• 서버 내부 보안 (SQL Injection 등)
• 악성 사이트인지 여부
```

---

## Let's Encrypt 사용하기

### Certbot으로 인증서 발급

```bash
# 설치 (Ubuntu)
sudo apt update
sudo apt install certbot python3-certbot-nginx

# Nginx용 인증서 발급 + 자동 설정
sudo certbot --nginx -d api.example.com

# 인증서만 발급 (수동 설정)
sudo certbot certonly --standalone -d api.example.com

# 인증서 갱신 테스트
sudo certbot renew --dry-run
```

### 발급된 파일

```
/etc/letsencrypt/live/api.example.com/
├── fullchain.pem    ← 인증서 + 중간 인증서 (Nginx에서 사용)
├── privkey.pem      ← 개인키 (절대 유출 금지!)
├── cert.pem         ← 서버 인증서만
└── chain.pem        ← 중간 인증서만
```

### 자동 갱신 설정

```bash
# Crontab에 추가 (매일 새벽 3시 체크)
0 3 * * * certbot renew --quiet --post-hook "systemctl reload nginx"
```

---

## 핵심 정리

1. **SSL은 폐기됨, TLS가 현재 표준**
   - 실무에서 "SSL"이라 불러도 실제론 TLS 1.2/1.3 사용

2. **HTTPS = HTTP + TLS**
   - 기밀성, 무결성, 인증 제공

3. **비대칭키로 세션키 교환 → 대칭키로 데이터 암호화**
   - 비대칭키: 안전하지만 느림 (키 교환용)
   - 대칭키: 빠름 (실제 데이터 암호화)

4. **인증서 = 서버 신분증**
   - CA가 서명해서 신뢰성 보장
   - 서버 공개키 포함

5. **복호화는 SSL 종료점(Nginx)에서만**
   - Nginx가 TLS 처리 → WAS는 평문 HTTP
   - 개인키는 서버에만 존재 (절대 유출 금지)

6. **공개키 탈취는 무의미**
   - 암호화만 가능, 복호화 불가
   - 개인키 없으면 세션키 복호화 못함

---

## 관련 노트

- [웹 서버 동작 원리](web-server-basics.md) - HTTP 요청 처리 과정
- [Nginx 완벽 가이드](nginx-guide.md) - SSL 설정, 리버스 프록시

---

## 참고 자료

- [SSL과 TLS의 차이 | AWS](https://aws.amazon.com/ko/compare/the-difference-between-ssl-and-tls/)
- [TLS 핸드셰이크 과정 | Cloudflare](https://www.cloudflare.com/ko-kr/learning/ssl/what-happens-in-a-tls-handshake/)
- [HTTPS가 왜 안전한가 | Cloudflare](https://www.cloudflare.com/ko-kr/learning/ssl/why-is-http-not-secure/)
- [공개키/개인키 암호화 | Velog](https://babbab2.tistory.com/4)
- [HTTPS 완전 정복 | Velog](https://junghwan17.tistory.com/entry/HTTPS-완전-정복-TLS-인증서-CA-그리고-숨겨진-보안-문제들)
- [SSL/TLS 복호화 위치 | Velog](https://velog.io/@kti0940/HTTPS-SSL-복호화는-어디서-진행될까)
- [SSL 인증서 위조 공격 | Seqrite](https://www.seqrite.com/ko/blog/how-fraudulent-ssl-certificates-could-be-utilized-to-hack-your-website/)
- [Self-Signed 인증서 | SSL.com](https://www.ssl.com/ko/기사/SSL-tls-self-signed-certificates/)

---

*마지막 업데이트: 2025-12-21*
