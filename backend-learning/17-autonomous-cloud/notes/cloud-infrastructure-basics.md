# 클라우드 인프라 기초 가이드

> 주니어 개발자를 위한 AWS 핵심 서비스 완벽 정리

## 목차

1. [클라우드 컴퓨팅 기초](#클라우드-컴퓨팅-기초)
2. [AWS 핵심 서비스 개요](#aws-핵심-서비스-개요)
3. [EC2 - 가상 서버](#ec2---가상-서버)
4. [ECS - 컨테이너 오케스트레이션](#ecs---컨테이너-오케스트레이션)
5. [ECR - 컨테이너 레지스트리](#ecr---컨테이너-레지스트리)
6. [기타 필수 서비스](#기타-필수-서비스)
7. [실전 배포 시나리오](#실전-배포-시나리오)
8. [DevOps 학습 로드맵](#devops-학습-로드맵)

---

## 클라우드 컴퓨팅 기초

### 클라우드란?

물리적 서버를 직접 구매하고 관리하는 대신, 인터넷을 통해 필요한 만큼 컴퓨팅 리소스를 빌려 쓰는 방식

### 전통적 방식 vs 클라우드

```
┌─────────────────────────────────────────────────────────────┐
│              온프레미스 (Traditional)                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  서버 구매 (수백만원) → 데이터센터 설치 → 네트워크 구성     │
│    → OS 설치 → 보안 설정 → 애플리케이션 배포               │
│                                                             │
│  💰 초기 비용: 매우 높음                                    │
│  ⏱️  구축 시간: 수 주 ~ 수 개월                             │
│  📈 확장성: 어려움 (새 서버 구매 필요)                      │
│  🔧 운영: 직접 관리 필요 (전력, 냉각, 하드웨어 교체 등)     │
│                                                             │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                   클라우드 (AWS, GCP, Azure)                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  AWS 콘솔 접속 → 몇 번의 클릭 → 서버 생성 완료             │
│    → 애플리케이션 배포                                      │
│                                                             │
│  💰 초기 비용: 거의 없음 (사용한 만큼만 지불)               │
│  ⏱️  구축 시간: 수 분                                       │
│  📈 확장성: 매우 쉬움 (클릭 몇 번 or 자동)                  │
│  🔧 운영: 인프라는 클라우드가 관리, 애플리케이션만 관리     │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 클라우드 서비스 모델

```
┌─────────────────────────────────────────────────────────────┐
│                  책임 범위 비교                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  온프레미스     IaaS         PaaS         SaaS              │
│  (모두 직접)   (EC2)         (Heroku)     (Gmail)           │
│                                                             │
│  [애플리케이션] [애플리케이션] [애플리케이션] [제공됨]       │
│  [데이터]      [데이터]      [데이터]      [제공됨]         │
│  [런타임]      [런타임]      [제공됨]      [제공됨]         │
│  [미들웨어]    [미들웨어]    [제공됨]      [제공됨]         │
│  [OS]         [OS]          [제공됨]      [제공됨]         │
│  [가상화]      [제공됨]      [제공됨]      [제공됨]         │
│  [서버]        [제공됨]      [제공됨]      [제공됨]         │
│  [스토리지]    [제공됨]      [제공됨]      [제공됨]         │
│  [네트워크]    [제공됨]      [제공됨]      [제공됨]         │
│                                                             │
│  관리 부담: 높음 ← ─────────────────────── → 낮음           │
│  유연성:   높음 ← ─────────────────────── → 낮음           │
│                                                             │
└─────────────────────────────────────────────────────────────┘

IaaS (Infrastructure as a Service):
  - 가상 서버, 스토리지, 네트워크 제공
  - 예: AWS EC2, GCP Compute Engine

PaaS (Platform as a Service):
  - 개발 플랫폼 제공 (런타임, DB, 웹서버 등)
  - 예: Heroku, AWS Elastic Beanstalk

SaaS (Software as a Service):
  - 완성된 소프트웨어 제공
  - 예: Gmail, Slack, Notion
```

---

## AWS 핵심 서비스 개요

### 주요 카테고리

```
AWS 서비스 맵 (DevOps 관점)

컴퓨팅                  컨테이너                 스토리지
├── EC2                ├── ECS                 ├── S3
├── Lambda             ├── ECR                 ├── EBS
└── Lightsail          ├── EKS                 └── EFS
                       └── Fargate

네트워킹                데이터베이스              모니터링
├── VPC                ├── RDS                 ├── CloudWatch
├── Route 53           ├── DynamoDB            ├── X-Ray
├── ELB                └── ElastiCache         └── CloudTrail
└── CloudFront

보안                    CI/CD                   IaC
├── IAM                ├── CodePipeline        ├── CloudFormation
├── Security Groups    ├── CodeBuild           └── CDK
└── ACM                └── CodeDeploy
```

---

## EC2 - 가상 서버

### EC2란?

**Elastic Compute Cloud** - AWS의 가상 서버 서비스

쉽게 말해, 클라우드에서 임대하는 컴퓨터입니다. 원하는 사양(CPU, 메모리, 스토리지)을 선택하여 몇 분 만에 서버를 생성할 수 있습니다.

### 핵심 개념

#### 1. 인스턴스 타입

```
인스턴스 타입 네이밍: t3.medium
                     │ │  │
                     │ │  └─ 크기 (nano, small, medium, large, xlarge ...)
                     │ └──── 세대 (숫자가 클수록 최신)
                     └────── 타입 패밀리

주요 타입 패밀리:
├── t (T-series): 범용, 버스트 가능, 가성비 좋음
│   예: t3.micro - 웹 서버, 개발 환경
│
├── m (Memory): 균형잡힌 컴퓨팅/메모리
│   예: m5.large - 일반적인 애플리케이션
│
├── c (Compute): CPU 집약적
│   예: c5.xlarge - 고성능 컴퓨팅, 게임 서버
│
├── r (RAM): 메모리 집약적
│   예: r5.2xlarge - 인메모리 DB, 캐시
│
└── g (GPU): GPU 필요 작업
    예: g4dn.xlarge - 머신러닝, 영상 처리
```

#### 2. AMI (Amazon Machine Image)

서버의 스냅샷/템플릿

```
AMI = OS + 소프트웨어 + 설정

┌─────────────────────────────────────┐
│         AMI 구성                     │
├─────────────────────────────────────┤
│                                     │
│  운영체제 (Ubuntu, Amazon Linux, …) │
│  + 설치된 소프트웨어 (Node.js, …)   │
│  + 시스템 설정                       │
│  + 애플리케이션 코드 (선택사항)      │
│                                     │
└─────────────────────────────────────┘

사용 시나리오:
1. AWS 제공 AMI 사용 (Ubuntu, Amazon Linux)
2. 커스텀 AMI 생성 (자주 쓰는 설정을 템플릿화)
3. 마켓플레이스 AMI (NGINX, WordPress 등 사전 설치)
```

#### 3. 보안 그룹 (Security Group)

EC2의 방화벽 역할

```
┌─────────────────────────────────────────────────────────┐
│              Security Group 예시                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Inbound Rules (들어오는 트래픽):                        │
│  ┌─────────┬──────┬─────────┬────────────────┐         │
│  │  Type   │ Port │ Source  │   설명         │         │
│  ├─────────┼──────┼─────────┼────────────────┤         │
│  │  HTTP   │  80  │ 0.0.0.0/0│ 전세계 접근 OK│         │
│  │  HTTPS  │ 443  │ 0.0.0.0/0│ 전세계 접근 OK│         │
│  │  SSH    │  22  │ 내 IP만  │ 관리용         │         │
│  │  Custom │ 3000 │ VPC 내부 │ 앱 간 통신     │         │
│  └─────────┴──────┴─────────┴────────────────┘         │
│                                                         │
│  Outbound Rules (나가는 트래픽):                         │
│  기본값: 모든 트래픽 허용                                │
│                                                         │
└─────────────────────────────────────────────────────────┘

보안 팁:
❌ SSH (22번 포트)를 0.0.0.0/0으로 열지 말 것!
✅ 특정 IP 또는 VPN에서만 접근 허용
✅ 필요한 포트만 열기
```

#### 4. EBS (Elastic Block Store)

EC2에 연결하는 하드디스크

```
EBS 볼륨 타입:

gp3 (General Purpose SSD):
  - 대부분의 워크로드에 적합
  - 가성비 최고
  - 용도: OS, 애플리케이션, 일반 DB

io2 (Provisioned IOPS SSD):
  - 고성능 필요 시
  - 비싸지만 빠름
  - 용도: 프로덕션 DB, 고성능 앱

st1 (Throughput Optimized HDD):
  - 대용량 데이터, 순차 접근
  - 용도: 로그, 데이터 웨어하우스

sc1 (Cold HDD):
  - 가장 저렴
  - 용도: 백업, 아카이브
```

### EC2 실전 사용 예시

#### 웹 서버 배포하기

```bash
# 1. EC2 인스턴스 접속
ssh -i "mykey.pem" ec2-user@ec2-xx-xx-xx-xx.compute.amazonaws.com

# 2. 업데이트 및 Node.js 설치
sudo yum update -y
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20

# 3. 애플리케이션 배포
git clone https://github.com/myapp.git
cd myapp
npm install
npm run build

# 4. PM2로 프로세스 관리
npm install -g pm2
pm2 start npm --name "myapp" -- start
pm2 save
pm2 startup
```

### EC2 가격 모델

```
┌──────────────────────────────────────────────────────────┐
│              EC2 가격 옵션                                │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  On-Demand (온디맨드):                                    │
│  - 사용한 만큼 시간당 과금                                │
│  - 가장 비싸지만 유연함                                   │
│  - 용도: 개발/테스트, 예측 불가능한 워크로드              │
│  💰 예: t3.medium $0.0416/시간 (~$30/월)                │
│                                                          │
│  Reserved (예약 인스턴스):                                │
│  - 1년 또는 3년 약정                                      │
│  - 최대 72% 할인                                         │
│  - 용도: 안정적으로 계속 사용하는 프로덕션 서버           │
│  💰 예: t3.medium $0.0125/시간 (~$9/월, 3년 약정 시)    │
│                                                          │
│  Spot (스팟 인스턴스):                                    │
│  - 경매 방식, 최대 90% 할인                              │
│  - 언제든 중단될 수 있음                                  │
│  - 용도: 배치 작업, 빅데이터 처리, CI/CD                 │
│  💰 예: t3.medium $0.0125/시간 (변동)                   │
│                                                          │
└──────────────────────────────────────────────────────────┘

추천:
- 프로덕션: Reserved (1년 이상 사용 확실하면)
- 개발: On-Demand
- 배치작업: Spot
```

---

## ECS - 컨테이너 오케스트레이션

### ECS란?

**Elastic Container Service** - AWS의 컨테이너 관리 서비스

Docker 컨테이너를 대규모로 실행하고 관리하는 서비스입니다. Kubernetes의 AWS 버전이라고 생각하면 됩니다 (실제로는 EKS가 Kubernetes이지만, ECS가 더 간단합니다).

### 핵심 개념

```
┌─────────────────────────────────────────────────────────┐
│              ECS 아키텍처                                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Cluster (클러스터)                                      │
│  └─ 컨테이너들을 실행하는 논리적 그룹                     │
│                                                         │
│     ├─ Service (서비스)                                 │
│     │  └─ Task를 계속 실행하고 관리                     │
│     │     ├─ Auto-scaling 설정                         │
│     │     ├─ Load Balancer 연결                        │
│     │     └─ 원하는 Task 개수 유지                      │
│     │                                                   │
│     └─ Task (태스크)                                    │
│        └─ 실제 컨테이너 실행 단위                        │
│           ├─ Task Definition 기반                      │
│           └─ 1개 이상의 컨테이너 포함                   │
│                                                         │
│  Task Definition (태스크 정의)                           │
│  └─ 컨테이너 실행 설정의 청사진                          │
│     ├─ Docker 이미지                                    │
│     ├─ CPU/메모리 할당                                  │
│     ├─ 환경 변수                                        │
│     ├─ 포트 매핑                                        │
│     └─ 볼륨 마운트                                      │
│                                                         │
└─────────────────────────────────────────────────────────┘

비유:
- Cluster = 공장
- Service = 생산 라인 (자동으로 제품 생산 유지)
- Task = 실제 작동 중인 기계
- Task Definition = 기계 설계도
```

### Launch Type: EC2 vs Fargate

```
┌──────────────────────────────────────────────────────────┐
│          ECS on EC2 vs ECS Fargate                       │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ECS on EC2:                                             │
│  ┌────────────────────────────────────────┐             │
│  │  EC2 인스턴스 (직접 관리)               │             │
│  │  ├─ Container 1                        │             │
│  │  ├─ Container 2                        │             │
│  │  └─ Container 3                        │             │
│  └────────────────────────────────────────┘             │
│  장점: 비용 최적화 가능, 세밀한 제어         │             │
│  단점: EC2 관리 필요 (패치, 스케일링 등)     │             │
│  용도: 고정적 워크로드, 비용 최적화 중요 시  │             │
│                                                          │
│  ECS Fargate:                                            │
│  ┌────────────────────────────────────────┐             │
│  │  서버리스 (AWS가 관리)                  │             │
│  │  ├─ Container 1 (격리됨)                │             │
│  │  ├─ Container 2 (격리됨)                │             │
│  │  └─ Container 3 (격리됨)                │             │
│  └────────────────────────────────────────┘             │
│  장점: 서버 관리 불필요, 빠른 스케일링       │             │
│  단점: EC2보다 비쌈                         │             │
│  용도: 가변적 워크로드, 운영 부담 줄이기     │             │
│                                                          │
└──────────────────────────────────────────────────────────┘

추천:
- 시작: Fargate (관리 간편)
- 성숙 단계: EC2 (비용 최적화)
```

### Task Definition 예시

```json
{
  "family": "my-web-app",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "containerDefinitions": [
    {
      "name": "web",
      "image": "123456789.dkr.ecr.ap-northeast-2.amazonaws.com/my-app:latest",
      "portMappings": [
        {
          "containerPort": 3000,
          "protocol": "tcp"
        }
      ],
      "environment": [
        {
          "name": "NODE_ENV",
          "value": "production"
        },
        {
          "name": "PORT",
          "value": "3000"
        }
      ],
      "secrets": [
        {
          "name": "DB_PASSWORD",
          "valueFrom": "arn:aws:secretsmanager:region:account:secret:db-password"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/my-web-app",
          "awslogs-region": "ap-northeast-2",
          "awslogs-stream-prefix": "web"
        }
      },
      "healthCheck": {
        "command": ["CMD-SHELL", "curl -f http://localhost:3000/health || exit 1"],
        "interval": 30,
        "timeout": 5,
        "retries": 3
      }
    }
  ]
}
```

### ECS Service 생성 흐름

```
1. Task Definition 생성
   └─ 컨테이너 이미지, 리소스, 환경변수 정의

2. Cluster 생성
   └─ Fargate 또는 EC2 선택

3. Service 생성
   ├─ Task Definition 선택
   ├─ 원하는 Task 개수 지정 (예: 3개)
   ├─ Load Balancer 연결 (선택사항)
   ├─ Auto Scaling 설정
   └─ 네트워크 설정 (VPC, 서브넷, 보안그룹)

4. 배포 완료
   └─ ECS가 자동으로 Task 실행 및 관리
```

---

## ECR - 컨테이너 레지스트리

### ECR이란?

**Elastic Container Registry** - AWS의 Docker 이미지 저장소

Docker Hub의 프라이빗 버전이라고 생각하면 됩니다. ECS/EKS에서 사용할 Docker 이미지를 안전하게 저장하고 관리합니다.

### 핵심 개념

```
┌─────────────────────────────────────────────────────────┐
│              ECR Repository                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Repository: my-app                                     │
│  ├─ latest (tag)                                        │
│  │  └─ sha256:abc123... (image digest)                 │
│  ├─ v1.0.0                                              │
│  │  └─ sha256:def456...                                │
│  ├─ v1.1.0                                              │
│  │  └─ sha256:ghi789...                                │
│  └─ dev-2024-01-03                                      │
│     └─ sha256:jkl012...                                │
│                                                         │
│  주요 설정:                                              │
│  ├─ Image scanning: 취약점 자동 스캔                     │
│  ├─ Lifecycle policy: 오래된 이미지 자동 삭제            │
│  ├─ Encryption: 저장 데이터 암호화                       │
│  └─ IAM permissions: 접근 권한 관리                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### ECR 사용 워크플로우

```bash
# 1. ECR 로그인
aws ecr get-login-password --region ap-northeast-2 | \
  docker login --username AWS --password-stdin \
  123456789.dkr.ecr.ap-northeast-2.amazonaws.com

# 2. Docker 이미지 빌드
docker build -t my-app:latest .

# 3. ECR 태그 추가
docker tag my-app:latest \
  123456789.dkr.ecr.ap-northeast-2.amazonaws.com/my-app:latest

# 4. ECR에 푸시
docker push \
  123456789.dkr.ecr.ap-northeast-2.amazonaws.com/my-app:latest

# 5. ECS에서 사용
# Task Definition에서 위 이미지 URI 지정
```

### Lifecycle Policy 예시

오래된 이미지를 자동으로 삭제하여 비용 절감

```json
{
  "rules": [
    {
      "rulePriority": 1,
      "description": "Keep only 10 latest images",
      "selection": {
        "tagStatus": "any",
        "countType": "imageCountMoreThan",
        "countNumber": 10
      },
      "action": {
        "type": "expire"
      }
    },
    {
      "rulePriority": 2,
      "description": "Remove untagged images older than 7 days",
      "selection": {
        "tagStatus": "untagged",
        "countType": "sinceImagePushed",
        "countUnit": "days",
        "countNumber": 7
      },
      "action": {
        "type": "expire"
      }
    }
  ]
}
```

---

## 기타 필수 서비스

### VPC (Virtual Private Cloud)

AWS의 가상 네트워크 - 서버들의 격리된 네트워크 공간

```
┌──────────────────────────────────────────────────────────┐
│                  VPC 구조                                 │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  VPC (10.0.0.0/16)                                       │
│  ├─ Public Subnet (10.0.1.0/24)  [가용영역 A]            │
│  │  ├─ 인터넷 게이트웨이 연결                            │
│  │  ├─ ELB (Load Balancer)                              │
│  │  └─ NAT Gateway                                      │
│  │                                                       │
│  ├─ Private Subnet (10.0.10.0/24) [가용영역 A]           │
│  │  ├─ 인터넷 직접 접근 불가                             │
│  │  ├─ EC2 / ECS Tasks                                  │
│  │  └─ NAT Gateway를 통해서만 외부 통신                  │
│  │                                                       │
│  ├─ Public Subnet (10.0.2.0/24)  [가용영역 B]            │
│  │  └─ 고가용성을 위한 중복 구성                         │
│  │                                                       │
│  └─ Private Subnet (10.0.20.0/24) [가용영역 B]           │
│     └─ 고가용성을 위한 중복 구성                          │
│                                                          │
└──────────────────────────────────────────────────────────┘

보안 계층:
Public Subnet: 인터넷에 노출되어야 하는 리소스 (웹서버, LB)
Private Subnet: 내부 리소스만 (앱 서버, DB)

고가용성:
최소 2개 이상의 가용영역(AZ) 사용
```

### ELB (Elastic Load Balancer)

트래픽을 여러 서버에 분산

```
┌──────────────────────────────────────────────────────────┐
│              Load Balancer 종류                           │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ALB (Application Load Balancer):                        │
│  ├─ HTTP/HTTPS 트래픽용                                  │
│  ├─ Layer 7 (애플리케이션 계층)                          │
│  ├─ 경로 기반 라우팅 (예: /api → 서버A, /web → 서버B)    │
│  ├─ 호스트 기반 라우팅 (예: api.example.com, www...)    │
│  └─ 용도: 웹 애플리케이션, 마이크로서비스                │
│                                                          │
│  NLB (Network Load Balancer):                            │
│  ├─ TCP/UDP 트래픽용                                     │
│  ├─ Layer 4 (전송 계층)                                  │
│  ├─ 초고성능 (수백만 req/sec)                            │
│  ├─ 고정 IP 지원                                         │
│  └─ 용도: 게임 서버, IoT, 실시간 통신                    │
│                                                          │
│  CLB (Classic Load Balancer):                            │
│  └─ 레거시, 새 프로젝트는 사용하지 말 것                  │
│                                                          │
└──────────────────────────────────────────────────────────┘

대부분의 경우: ALB 사용
```

### RDS (Relational Database Service)

관리형 데이터베이스 서비스

```
지원 DB 엔진:
├── PostgreSQL (추천)
├── MySQL
├── MariaDB
├── Oracle
├── SQL Server
└── Aurora (AWS 자체 DB, MySQL/PostgreSQL 호환)

주요 기능:
✅ 자동 백업 (특정 시점 복구 가능)
✅ 자동 패치 및 업데이트
✅ Multi-AZ (고가용성)
✅ Read Replica (읽기 부하 분산)
✅ 자동 모니터링

EC2에 직접 DB 설치 vs RDS:
- 직접 설치: 완전한 제어, 비용 저렴
- RDS: 관리 편함, 안정성 높음

→ 프로덕션은 RDS 강력 추천
```

### S3 (Simple Storage Service)

오브젝트 스토리지 - 파일 저장소

```
사용 사례:
├── 정적 파일 호스팅 (HTML, CSS, JS)
├── 이미지/동영상 저장
├── 백업 및 아카이브
├── 로그 파일 저장
└── 빅데이터 저장

특징:
✅ 무제한 용량
✅ 99.999999999% (11 9s) 내구성
✅ 버저닝 지원 (파일 이력 관리)
✅ 저렴한 비용 ($0.023/GB)
✅ CDN(CloudFront)과 통합

Storage Class:
- S3 Standard: 자주 접근하는 데이터
- S3 IA (Infrequent Access): 가끔 접근
- S3 Glacier: 아카이브 (매우 저렴)
```

### CloudWatch

모니터링 및 로그 서비스

```
주요 기능:

1. Metrics (메트릭):
   ├── EC2: CPU, 네트워크, 디스크
   ├── RDS: 커넥션 수, 쿼리 성능
   ├── ELB: 요청 수, 응답 시간
   └── 커스텀 메트릭: 애플리케이션 지표

2. Logs (로그):
   ├── 중앙 집중식 로그 수집
   ├── 로그 검색 및 필터링
   ├── 로그 보관 기간 설정
   └── Lambda로 자동 처리 가능

3. Alarms (알람):
   ├── 임계값 설정 (예: CPU > 80%)
   ├── SNS로 이메일/SMS 알림
   └── Auto Scaling 트리거

4. Dashboards:
   └── 커스텀 모니터링 대시보드
```

### IAM (Identity and Access Management)

AWS 권한 관리

```
핵심 개념:

User (사용자):
  └─ 개인 또는 애플리케이션

Group (그룹):
  └─ 사용자들의 모음 (예: Developers, Admins)

Role (역할):
  └─ AWS 리소스에 부여하는 임시 권한
     예: EC2가 S3에 접근할 수 있는 권한

Policy (정책):
  └─ 권한 규칙을 JSON으로 정의

예시 Policy:
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "s3:GetObject",
        "s3:PutObject"
      ],
      "Resource": "arn:aws:s3:::my-bucket/*"
    }
  ]
}

보안 원칙:
✅ 최소 권한 원칙 (필요한 것만)
✅ 루트 계정 사용 금지
✅ MFA (다중 인증) 활성화
✅ 정기적 권한 검토
```

---

## 실전 배포 시나리오

### 시나리오 1: 간단한 웹 애플리케이션 (Node.js)

```
아키텍처:
Internet → ALB → ECS Fargate → RDS PostgreSQL
                     ↓
                    ECR

구성 요소:
1. ECR: Docker 이미지 저장
2. ECS Fargate: 컨테이너 실행
3. ALB: 로드 밸런싱
4. RDS: 데이터베이스
5. CloudWatch: 로그 및 모니터링
```

#### 배포 단계

```bash
# Step 1: Dockerfile 작성
cat > Dockerfile <<EOF
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["node", "server.js"]
EOF

# Step 2: ECR Repository 생성
aws ecr create-repository --repository-name my-app

# Step 3: 이미지 빌드 및 푸시
docker build -t my-app .
docker tag my-app:latest 123456789.dkr.ecr.ap-northeast-2.amazonaws.com/my-app:latest
docker push 123456789.dkr.ecr.ap-northeast-2.amazonaws.com/my-app:latest

# Step 4: RDS 생성 (AWS Console 또는 CLI)
aws rds create-db-instance \
  --db-instance-identifier myapp-db \
  --db-instance-class db.t3.micro \
  --engine postgres \
  --master-username admin \
  --master-user-password MySecurePassword123 \
  --allocated-storage 20

# Step 5: ECS Task Definition 생성
# (위에서 본 JSON 사용)

# Step 6: ECS Service 생성
aws ecs create-service \
  --cluster my-cluster \
  --service-name my-app-service \
  --task-definition my-web-app:1 \
  --desired-count 2 \
  --launch-type FARGATE \
  --load-balancers targetGroupArn=arn:...,containerName=web,containerPort=3000

# Step 7: Route 53에서 도메인 설정
# ALB DNS를 도메인에 연결
```

### 시나리오 2: CI/CD 파이프라인

GitHub Actions를 통한 자동 배포

```yaml
# .github/workflows/deploy.yml
name: Deploy to ECS

on:
  push:
    branches: [main]

env:
  AWS_REGION: ap-northeast-2
  ECR_REPOSITORY: my-app
  ECS_SERVICE: my-app-service
  ECS_CLUSTER: my-cluster
  ECS_TASK_DEFINITION: my-web-app

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build, tag, and push image to ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Download task definition
        run: |
          aws ecs describe-task-definition \
            --task-definition ${{ env.ECS_TASK_DEFINITION }} \
            --query taskDefinition > task-definition.json

      - name: Fill in the new image ID in the task definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: task-definition.json
          container-name: web
          image: ${{ steps.build-image.outputs.image }}

      - name: Deploy to Amazon ECS
        uses: aws-actions/amazon-ecs-deploy-task-definition@v1
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ${{ env.ECS_SERVICE }}
          cluster: ${{ env.ECS_CLUSTER }}
          wait-for-service-stability: true
```

**워크플로우:**
```
Git Push → GitHub Actions → Docker Build → ECR Push
    → ECS Task Definition 업데이트 → ECS Service 배포
```

### 시나리오 3: Auto Scaling 설정

```bash
# Target Tracking Scaling (CPU 기반)
aws application-autoscaling register-scalable-target \
  --service-namespace ecs \
  --scalable-dimension ecs:service:DesiredCount \
  --resource-id service/my-cluster/my-app-service \
  --min-capacity 2 \
  --max-capacity 10

aws application-autoscaling put-scaling-policy \
  --service-namespace ecs \
  --scalable-dimension ecs:service:DesiredCount \
  --resource-id service/my-cluster/my-app-service \
  --policy-name cpu-target-tracking \
  --policy-type TargetTrackingScaling \
  --target-tracking-scaling-policy-configuration file://scaling-policy.json

# scaling-policy.json
{
  "TargetValue": 70.0,
  "PredefinedMetricSpecification": {
    "PredefinedMetricType": "ECSServiceAverageCPUUtilization"
  },
  "ScaleInCooldown": 300,
  "ScaleOutCooldown": 60
}
```

**동작 원리:**
- CPU 사용률 70% 초과 → Task 추가
- CPU 사용률 70% 미만 → Task 제거
- 최소 2개, 최대 10개 Task 유지

---

## DevOps 학습 로드맵

### 레벨 1: 기초 (1-2개월)

```
Week 1-2: 클라우드 기본 이해
□ AWS 계정 생성 및 Free Tier 활용
□ EC2 인스턴스 생성 및 SSH 접속
□ 보안 그룹 설정
□ 간단한 웹 서버 배포 (Nginx, Node.js)

Week 3-4: 네트워킹 기초
□ VPC 개념 이해
□ Public/Private 서브넷 구성
□ Route Table, Internet Gateway
□ 보안 그룹 vs NACL

Week 5-6: 스토리지 및 데이터베이스
□ S3 버킷 생성 및 파일 업로드
□ EBS 볼륨 연결
□ RDS 데이터베이스 생성
□ 애플리케이션을 RDS에 연결

Week 7-8: 모니터링 및 IAM
□ CloudWatch 메트릭 확인
□ CloudWatch Logs 설정
□ Alarm 생성
□ IAM User, Role, Policy 생성

프로젝트: EC2에 블로그 애플리케이션 배포
```

### 레벨 2: 컨테이너화 (2-3개월)

```
Week 1-2: Docker 기초
□ Docker 설치 및 기본 명령어
□ Dockerfile 작성
□ 이미지 빌드 및 실행
□ Docker Compose로 멀티 컨테이너

Week 3-4: ECR 및 ECS 입문
□ ECR Repository 생성
□ Docker 이미지 푸시
□ ECS Cluster 생성
□ Fargate로 첫 Task 실행

Week 5-6: ECS 심화
□ Task Definition 최적화
□ ECS Service 생성
□ ALB와 ECS 연동
□ Auto Scaling 설정

Week 7-8: 로그 및 모니터링
□ CloudWatch Logs 통합
□ X-Ray로 분산 추적
□ Container Insights

프로젝트: 마이크로서비스 애플리케이션을 ECS에 배포
```

### 레벨 3: CI/CD 및 자동화 (2-3개월)

```
Week 1-2: Git 및 GitHub
□ Git 브랜치 전략 (Git Flow)
□ Pull Request 워크플로우
□ GitHub Actions 기초

Week 3-4: CI/CD 파이프라인
□ GitHub Actions로 Docker 빌드
□ 자동 테스트 실행
□ ECR에 자동 푸시
□ ECS 자동 배포

Week 5-6: Infrastructure as Code
□ Terraform 기초
□ AWS 리소스를 코드로 정의
□ State 관리
□ Terraform 모듈화

Week 7-8: 배포 전략
□ Blue-Green Deployment
□ Canary Deployment
□ Rollback 전략

프로젝트: 완전 자동화된 CI/CD 파이프라인 구축
```

### 레벨 4: 고급 DevOps (3-6개월)

```
Month 1: Kubernetes 기초
□ Kubernetes 아키텍처 이해
□ EKS 클러스터 생성
□ Pod, Deployment, Service
□ Helm 차트

Month 2: 보안
□ AWS Secrets Manager
□ Parameter Store
□ Security scanning (Trivy, Snyk)
□ OWASP Top 10

Month 3: 고급 모니터링
□ Prometheus + Grafana
□ ELK Stack
□ APM (New Relic, Datadog)

Month 4-6: 실전 프로젝트
□ 프로덕션급 아키텍처 설계
□ 멀티 리전 배포
□ 재해 복구 계획
□ 비용 최적화

최종 프로젝트:
완전한 프로덕션 환경 구축
- Multi-AZ HA
- CI/CD
- 모니터링/알람
- 백업/복구
- 보안 강화
```

---

## 실전 팁 및 주의사항

### 비용 관리

```
💰 비용 절약 팁:

1. Free Tier 활용
   - EC2 t2.micro: 750시간/월 무료
   - RDS db.t2.micro: 750시간/월 무료
   - S3: 5GB 무료

2. 사용하지 않는 리소스 삭제
   ✅ 개발 완료 후 EC2 인스턴스 중지
   ✅ 사용하지 않는 EBS 볼륨 삭제
   ✅ 오래된 AMI 삭제
   ✅ 오래된 ECR 이미지 삭제

3. Billing Alarm 설정
   - CloudWatch로 예산 초과 시 알림
   - $10, $50 등 단계별 알람

4. Reserved Instance
   - 장기 사용 시 최대 72% 절약

5. Spot Instance
   - 배치 작업은 Spot으로 (90% 할인)

⚠️  실수로 큰 비용 나오는 경우:
- NAT Gateway 방치 ($0.045/시간)
- 멈춘 EC2 연결된 EBS 볼륨
- RDS 스냅샷 방치
- CloudWatch Logs 무제한 보관
```

### 보안 체크리스트

```
🔒 필수 보안 설정:

□ 루트 계정에 MFA 활성화
□ IAM 사용자별 개별 계정 생성
□ SSH 키 안전하게 관리 (.pem 파일 절대 공유 금지)
□ 보안 그룹 최소 권한 원칙
   ❌ 0.0.0.0/0으로 SSH(22) 오픈
   ✅ 특정 IP만 허용
□ RDS는 절대 Public 접근 금지
□ 민감 정보는 Secrets Manager 사용
□ S3 버킷 Public 접근 차단
□ CloudTrail 활성화 (감사 로그)
□ 정기적 IAM Access Analyzer 검토
```

### 고가용성 체크리스트

```
🏗️  프로덕션 아키텍처:

□ 최소 2개 이상의 가용영역 사용
□ Multi-AZ RDS (자동 failover)
□ ECS Service 최소 2개 Task
□ Auto Scaling 설정
□ Health Check 구성
□ Load Balancer 사용
□ 정기 백업 및 복구 테스트
□ Route 53 Failover Routing
```

---

## 주요 AWS CLI 명령어 치트시트

```bash
# EC2
aws ec2 describe-instances  # 인스턴스 목록
aws ec2 start-instances --instance-ids i-xxx  # 시작
aws ec2 stop-instances --instance-ids i-xxx   # 중지
aws ec2 terminate-instances --instance-ids i-xxx  # 종료

# ECS
aws ecs list-clusters  # 클러스터 목록
aws ecs list-services --cluster my-cluster  # 서비스 목록
aws ecs describe-services --cluster my-cluster --services my-service
aws ecs update-service --cluster my-cluster --service my-service --desired-count 3

# ECR
aws ecr describe-repositories  # 레포지토리 목록
aws ecr describe-images --repository-name my-app  # 이미지 목록
aws ecr get-login-password | docker login ...  # 로그인

# RDS
aws rds describe-db-instances  # DB 인스턴스 목록
aws rds create-db-snapshot --db-instance-identifier mydb --db-snapshot-identifier mydb-snapshot

# S3
aws s3 ls  # 버킷 목록
aws s3 cp file.txt s3://my-bucket/  # 업로드
aws s3 sync ./dist s3://my-bucket/  # 디렉토리 동기화

# CloudWatch
aws cloudwatch get-metric-statistics \
  --namespace AWS/EC2 \
  --metric-name CPUUtilization \
  --dimensions Name=InstanceId,Value=i-xxx \
  --start-time 2024-01-01T00:00:00Z \
  --end-time 2024-01-03T00:00:00Z \
  --period 3600 \
  --statistics Average

# Logs
aws logs tail /ecs/my-app --follow  # 실시간 로그
```

---

## 면접 대비 핵심 질문

### Q1: EC2와 ECS의 차이는?

```
A:
EC2는 가상 서버 자체를 제공하는 IaaS이고,
ECS는 컨테이너를 관리해주는 오케스트레이션 서비스입니다.

EC2:
- 직접 OS를 관리
- 애플리케이션을 직접 설치
- 확장성: Auto Scaling Group 설정 필요

ECS:
- 컨테이너 기반
- Docker 이미지만 준비하면 됨
- 확장성: Service Auto Scaling으로 쉽게 확장
- Fargate 사용 시 서버 관리 불필요
```

### Q2: ECS와 EKS 중 무엇을 선택해야 하나?

```
A:

ECS (Elastic Container Service):
✅ AWS 네이티브, 간단함
✅ Fargate로 서버리스 가능
✅ 러닝커브 낮음
❌ AWS에만 종속

EKS (Elastic Kubernetes Service):
✅ Kubernetes 표준
✅ 멀티 클라우드 가능
✅ 큰 커뮤니티
❌ 복잡함, 러닝커브 높음
❌ 비용이 더 높음

추천:
- AWS만 사용 + 빠른 시작: ECS
- 멀티 클라우드 + Kubernetes 경험: EKS
- 스타트업: ECS Fargate
```

### Q3: Public Subnet과 Private Subnet의 차이?

```
A:

Public Subnet:
- 인터넷 게이트웨이에 직접 연결
- 퍼블릭 IP 할당 가능
- 외부에서 직접 접근 가능
- 용도: Load Balancer, Bastion Host

Private Subnet:
- 인터넷 게이트웨이에 직접 연결 안 됨
- NAT Gateway를 통해서만 외부 통신
- 외부에서 직접 접근 불가
- 용도: 애플리케이션 서버, 데이터베이스

보안 관점에서 대부분의 리소스는 Private Subnet에 배치하고,
외부 트래픽은 Public Subnet의 Load Balancer가 받아서
Private Subnet의 애플리케이션으로 전달하는 구조가 권장됩니다.
```

### Q4: Blue-Green 배포란?

```
A:

두 개의 동일한 프로덕션 환경을 유지하는 배포 전략입니다.

Blue (현재 버전):
- 현재 트래픽을 처리 중인 환경

Green (새 버전):
- 새 버전을 배포한 환경

배포 과정:
1. Green 환경에 새 버전 배포
2. Green 환경 테스트
3. 로드 밸런서 트래픽을 Green으로 전환
4. 문제 발생 시 즉시 Blue로 롤백

장점:
✅ 즉시 롤백 가능
✅ 다운타임 없음
✅ 프로덕션과 동일한 환경에서 테스트

단점:
❌ 2배의 리소스 필요
❌ 데이터베이스 마이그레이션 복잡
```

---

## 다음 단계

이제 클라우드 인프라 기초를 이해했다면:

1. **직접 해보기**: 튜토리얼만 보지 말고 실제로 구축
2. **작은 프로젝트 배포**: 간단한 웹 앱을 ECS에 배포
3. **CI/CD 구축**: GitHub Actions로 자동 배포
4. **모니터링 설정**: CloudWatch 대시보드 구성
5. **비용 최적화**: Billing 확인하고 불필요한 리소스 삭제

**다음 학습 문서:**
- [Kubernetes 기초](./kubernetes-basics.md)
- [Terraform으로 IaC 시작하기](./terraform-getting-started.md)
- [AWS 보안 베스트 프랙티스](./aws-security-best-practices.md)

---

*최종 업데이트: 2026-01-03*
*작성자: Backend Learning Series*
